open-api-grpc-experiment
========================

An experiment to compare API construction with OpenAPI and gRPC.

Usage
-----

```sh
cd openapi
make deps install
```

And in separate terminals,

```sh
make serve
```

and

```sh
make query
```
